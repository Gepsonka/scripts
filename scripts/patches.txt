[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

# Strip phantom regular fields left over from the deleted scripts-module
# doctypes (BOM.main_fabric, Sales Order Item.fabric, etc.) so the
# fixtures/custom_field.json entries can create them as proper Custom
# Fields during the same migrate.
scripts.migrate.strip_phantom_standard_fields

# Restore stock ERPNext Sales Order: the app no longer ships any Sales
# Order / Sales Order Item fixtures, so delete leftover Custom Fields,
# Client Scripts and field_order layout overrides from existing sites.
# (Removing a fixture from JSON never deletes the DB record on its own.)
scripts.migrate.strip_sales_order_customizations

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
scripts.migrate.reattach_standard_erpnext_doctypes
